# Cloudflare Pages

# 项目名（控制台创建后填入）
CF_PROJECT_NAME=skill-studio

# Pages Functions / Workers API token（可选；用 wrangler login 也可）
# CF_API_TOKEN=

# 构建输出目录（Vite 默认 dist）
CF_OUTPUT_DIR=dist

# 入口命令和构建命令（CF 控制台 Build settings）
# Build command:  npm run build
# Build output:   dist
# Root directory: /
